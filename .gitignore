node_modules/
dist/
*.log
.DS_Store

# Local-only runtime state (never commit)
state/*.sqlite
state/*.sqlite-wal
state/*.sqlite-shm
state/*.log
state/*-state.json

# Local clones of the data repos (cloned at sibling paths via launchd jobs)
sherlock-context/
sherlock-vault/

# Editor + env
.env
.env.local
.vscode/
.idea/
